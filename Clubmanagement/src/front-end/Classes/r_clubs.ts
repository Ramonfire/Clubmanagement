class Rclubs extends compte{

    constructor(idE: number, civilite: number, fullname: String, email: String, password: String, tel: number) {
        super(idE, civilite, fullname, email, password, tel);
    }
}